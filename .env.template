# ── Exchange ──────────────────────────────────────────────────────────────────
# Sign up on Kraken: https://kraken.com (use referral: nsai.tech/kraken)
KRAKEN_API_KEY=YOUR_KRAKEN_API_KEY
KRAKEN_API_SECRET=YOUR_KRAKEN_API_SECRET

# ── AI Signal Scoring (free tier works) ───────────────────────────────────────
# Get your key at console.anthropic.com
ANTHROPIC_API_KEY=YOUR_ANTHROPIC_API_KEY

# ── Trading Config ────────────────────────────────────────────────────────────
PAPER_MODE=true           # Set to false when ready to go live
CAPITAL_USDT=25.0         # Starting capital in USDT
MAX_DRAWDOWN_PCT=8        # Pause trading if drawdown hits this %
GRID_LEVELS=10            # Number of grid levels
GRID_SPREAD_PCT=0.4       # Grid spacing %
TRI_ARB_MIN_PROFIT_PCT=0.35  # Min profit % to execute triangular arb

# ── NSAI Performance Fee (10% of profits — keeps the engine free) ─────────────
# This is how NSAI keeps Aegis free. 10% of each profitable trade is logged.
# To disable, set to 0. To support NSAI, leave at 10.
PERFORMANCE_FEE_PCT=10
